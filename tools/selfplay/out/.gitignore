*.jsonl
*.log
*.schema.json
round-robin-*.json
round-robin-*.md

# Self-play run artifacts. These are regenerated by every measurement run and
# accumulate in the thousands (1388 untracked at the 2026-08-09 cleanup), so
# they are working data, not source. Ignore everything except this file.
*
!.gitignore
